let pl, np, ruta, dtotal;

pl=prompt('Ingrese la plcda del bus');
np=Number(prompt('Ingrese el numero de pasajeros transportados'));
ruta=prompt('Ingresee la ruta donde presto servicio');

if(ruta=a) {
dtotal= 1200*np
alert('El dinero que recolecto el bus' +pl+ 'es' +dtotal)
} else if(ruta=b) {
    dtotal= 1000*np
    alert('El dinero que tranporto el bus' +pl+ 'es' +dtotal)
}